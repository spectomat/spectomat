---
description: "Show factory progress: iteration number, floor counts, plan steps, blocked files, log tail"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/command-status.sh:*)"]
---

# Spectomat status

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/command-status.sh"
```

Report and STOP.
