---
active: true
iteration: 1
session_id: {{SESSION_ID}}
max_iterations: {{MAX_ITERATIONS}}
started_at: "{{STARTED_AT}}"
---

# Pointer

Read ./docs/.spectomat/contract.md in full - it is the authoritative factory contract and may have been edited since your last iteration. Then follow its Loop Contract exactly: orient, pick exactly one phase of work (draft to spec, spec to plan, plan to task, plan to done), do it, verify, commit, log. One phase per iteration, then stop. Never ask the user anything.
